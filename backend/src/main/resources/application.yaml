spring:
  application:
    name: invoice

  datasource:
    url: jdbc:postgresql://localhost:5432/invoice_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
